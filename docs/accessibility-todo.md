# アクセシビリティ: 既知の課題(未対応)

B-9で対応した項目(モーダルのARIA属性・フォーカス管理・Escキー対応、選手一覧行のキーボード操作、ヘッダー検索欄のラベル・フォーカス表示)とは別に、調査の過程で見つかったが今回は対応を見送った項目。理由は「1PRで安全に直すには影響範囲が大きすぎる」「programmatic な検証手段がなく目視確認が必要」のいずれか。

## フォームラベルの未紐付け(未対応・影響大)

`src/components/ui/SegButton.tsx`の`FieldLabel`コンポーネントは、現状ただのスタイル付き`<div>`で、`<label for="...">`のように対応する入力欄とプログラム的に紐付いていない。アプリ全体のフォーム(設定画面・各種登録モーダル等、50箇所以上)がこのパターンを使っているため、スクリーンリーダーで「この入力欄は何の項目か」が読み上げられない。

対応するには`FieldLabel`に`htmlFor`(または`id`を子要素へ渡す形)を追加できるようにした上で、呼び出し側を1つずつ`id`/`htmlFor`のペアで結び直す必要があり、影響ファイル数が多く1PRで安全に検証しきれないため別途対応とする。`FieldLabel`側の後方互換なAPI追加(`htmlFor`省略時は現状通り`<div>`のまま)から始め、主要フォーム(チーム作成・招待・選手登録など利用頻度の高いもの)から順次移行するのが現実的。

## 色コントラストの網羅的な監査(未検証)

`--ink-soft`(補助テキスト)・`--orange`(アクセント、チームごとにカスタマイズ可能)等の配色が、WCAG AA基準(通常テキスト4.5:1、大きいテキスト3:1)を全画面で満たしているかは未検証。特に`teams.theme_primary`をチームが自由に設定できる仕様のため、チームが選んだ色によってはコントラスト不足になりうる(`--on-team-primary`はコントラスト安全な文字色を自動算出しているが、背景色そのものに対する周辺要素との比較までは検証していない)。

## スクリーンリーダーでの一連の操作確認(未実施)

VoiceOver/TalkBack等の実機スクリーンリーダーを使った、主要フロー(チーム作成→招待→出欠登録→試合記録)の通し確認は未実施。今回の修正はコード上の既知パターン(ARIA属性・キーボード操作)の是正に留まる。
